import java.util.Scanner;
/**
 * The program RecursiveArrayTwoSum implements a recursive algorithm 
 * that determines if an array of integers contains an integer that 
 * is the sum of two integers appearing earlier in the array.
 *
 * @author Jose Mojica Perez
 * @version 1.0
 * @since 2020-09-07
 */
public class RecursiveArrayTwoSum{
	public static void main(String[] args){
		int defaultIndex = 0;
		int[] arr = {1, 2, 3};
		System.out.println(arr, dafaultIndex);
	}
	public static boolean FindTwoSum (int[] arr, int target, int index){
		if(target - arr[index])
			return true;
		return true;
	}
}
